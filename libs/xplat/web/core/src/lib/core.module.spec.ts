describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { theinterviewCoreModule } from './core.module';

describe('theinterviewCoreModule', () => {
  it('should work', () => {
    expect(new theinterviewCoreModule(null)).toBeDefined();
  });
});
*/
